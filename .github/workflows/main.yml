<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <button onclick="document.getElementById('birthdaySong').play()">🎵 Play Music</button>
<audio id="birthdaySong" src="fav-song.mp3" loop></audio>

  <title>Birthday Greetings!</title>
  <script src="https://unpkg.com/lucide@latest"></script>
  <style>
    body {
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(to bottom, #ffd6e0, #fff);
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      flex-direction: column;
      overflow-x: hidden;
      min-height: 100vh;
      position: relative;
    }

    canvas#confetti {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      pointer-events: none;
      z-index: 0;
    }

 .cake-float {
  position: absolute;
  bottom: -50px; 
  font-size: 40px;
  opacity: 0.9;
  animation: float 10s linear infinite;
  z-index: 1;
}


@keyframes float {
  0% { transform: translateY(0) rotate(0deg); }
  100% { transform: translateY(-100vh) rotate(360deg); }
}

.cake-medium {
  font-size: 55px; 
  opacity: 0.95;
  animation: floatUpDown 6s ease-in-out infinite;
}

@keyframes float {
  0% { transform: translateY(0) rotate(0deg); }
  100% { transform: translateY(-120vh) rotate(360deg); }
}

@keyframes floatUpDown {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-20px); }
}


    /* 🩷 Card */
    .card {
      background: white;
      width: 90%;
      max-width: 600px;
      margin: 30px auto;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      overflow: hidden;
      z-index: 2;
    }

    .header {
      background: linear-gradient(135deg, #ff85a2, #ff5e91);
      color: white;
      padding: 15px;
      text-align: center;
      font-size: 18px;
      font-family: "Bookman Old Style", serif;
      font-weight: bold;
    }

    .sub-header {
      text-align: center;
      color: white;
      font-size: 12px;
      margin-top: 4px;
    }

    .content {
      padding: 15px;
      text-align: left;
      font-size: 14px;
      color: #333;
    }

    .content h2 {
      color: #e63946;
      font-size: 18px;
      margin: 10px 0;
      text-align: center;
    }

    .small-gray {
      color: gray;
      font-size: 12px;
      margin-bottom: 10px;
      text-align: center;
    }

    .plan {
      background: #fff7fa;
      border: 1px solid #eee;
      padding: 10px;
      border-radius: 8px;
      margin-top: 8px;
      font-size: 14px;
    }

    .plan strong {
      display: block;
      margin-bottom: 8px;
      font-size: 15px;
      font-weight: 600;
      text-align: center;
    }



  </style>
</head>
<body>
  <canvas id="confetti"></canvas>

<div class="cake-float" style="left: 5%; animation-delay: 0s;">🎂</div>
<div class="cake-float" style="left: 20%; animation-delay: 2s;">🎂</div>
<div class="cake-float" style="left: 40%; animation-delay: 4s;">🎂</div>
<div class="cake-float" style="left: 60%; animation-delay: 6s;">🎂</div>
<div class="cake-float" style="left: 80%; animation-delay: 8s;">🎂</div>


  <!-- 💌 Birthday Card -->
  <div class="card">
    <div class="header">
      happy birthday, lovey!!!
      <div class="sub-header">Today is October 17, 2025</div>
    </div>

    <div class="content">
      <p>Hi my lovey!</p>
      <h2>Friday, October 17, 2025</h2>
      <p class="small-gray">(happy 23rd!)</p>

      <div class="plan">
        <strong>to my dearest boyfriend,</strong>
        To my answered prayer, my constant day one, and my everlasting love, happy happy happy birthday!! My baby is officially 23! LOL, I knew how much you hate when your age adds up HAHAHAHAHA. But heyy! you're aging like fine wine, so really, there's nothing to be shy about hehe. Today is just another day of honoring you, my love, my best boy. I might never know what have I done in my past lives to deserve someone like you, but I'm really glad I did because I had you. And I'd get to love you. Fortunately, there's no turning back now it's gonna be you or no one else. Baby, thank you for having me as your girl because I got to experience a love that knows how to see my soul and heart. I wish nothing but for you to be happy and well always. I love you 4000.
      </div>
    </div>
  </div>

  <script>
    lucide.createIcons();

    const confettiCanvas = document.getElementById("confetti");
    const ctx = confettiCanvas.getContext("2d");
    confettiCanvas.width = window.innerWidth;
    confettiCanvas.height = window.innerHeight;

    const confettiPieces = Array.from({ length: 80 }).map(() => ({
      x: Math.random() * confettiCanvas.width,
      y: Math.random() * confettiCanvas.height - confettiCanvas.height,
      r: Math.random() * 6 + 4,
      d: Math.random() * 30 + 10,
      color: `hsl(${Math.random() * 360}, 100%, 70%)`,
    }));

    function drawConfetti() {
      ctx.clearRect(0, 0, confettiCanvas.width, confettiCanvas.height);
      confettiPieces.forEach(p => {
        ctx.fillStyle = p.color;
        ctx.fillRect(p.x, p.y, p.r, p.r);
        p.y += Math.cos(p.d) + 1 + p.r / 2;
        p.x += Math.sin(p.d);
        if (p.y > confettiCanvas.height) {
          p.y = -10;
          p.x = Math.random() * confettiCanvas.width;
        }
      });
      requestAnimationFrame(drawConfetti);
    }

    drawConfetti();
  </script>
</body>
</html>
